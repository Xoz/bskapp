import { Badge, Card, PageHeader } from "@/components/ui";
import { observations, skills } from "@/data/demo";

export default function MatchesPage() { return <div className="page"><PageHeader eyebrow="MATCH → TRÄNING" title="Matchobservationer"><button className="button primary">+ Ny observation</button></PageHeader><div className="grid two">{observations.map(observation => <Card key={observation.id} title={observation.match} meta={observation.occurredOn}><Badge tone={observation.sentiment === "positive" ? "green" : "amber"}>{observation.sentiment === "positive" ? "POSITIVT" : "BEHÖVER UTVECKLAS"}</Badge><p className="quote">“{observation.summary}”</p><div className="tags">{observation.skillIds.map(id => <span key={id}>{skills.find(s => s.id === id)?.name}</span>)}</div><button className="button">Skapa träningsfokus</button></Card>)}</div><Card title="Arbetsflöde" meta="Från observation till uppföljning"><div className="flow"><b>Observation</b><span>→</span><b>Färdighet</b><span>→</span><b>Övningar</b><span>→</span><b>Träningspass</b><span>→</span><b>Uppföljning</b></div></Card></div> }
