import { Badge, Card, PageHeader } from "@/components/ui";
import { periods, skills } from "@/data/demo";

export default function PlanningPage() { return <div className="page"><PageHeader eyebrow="SÄSONG 2026" title="Säsongsplan"><button className="button primary">+ Ny period</button></PageHeader><Card title="Säsongstidslinje" meta="April–oktober"><div className="timeline">{periods.map((period, index) => <article key={period.id} className={`period p${index + 1}`}><Badge tone={index === 2 ? "amber" : "green"}>{index === 2 ? "KOMMANDE" : "PLANERAD"}</Badge><h3>{period.name}</h3><p>{period.theme}</p><small>{period.startsOn} – {period.endsOn}</small></article>)}</div></Card><div className="grid two"><Card title="Pedagogisk fördelning" meta="7 mot 7 · riktlinje"><div className="allocation"><span style={{ width: "40%" }}>Teknik 40%</span><span style={{ width: "25%" }}>Passning 25%</span><span style={{ width: "20%" }}>Beslut 20%</span><span style={{ width: "15%" }}>Övrigt 15%</span></div></Card><Card title="Färdigheter i planen" meta={`${skills.length} prioriterade`}><div className="tags">{skills.map(skill => <span key={skill.id}>{skill.name}</span>)}</div></Card></div></div> }
